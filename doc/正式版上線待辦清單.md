# 正式版上線待辦清單

最後更新：2026-06-15

## 目前定案

- 正式執行版本以目前測試中的 GAS、LINE Bot、LIFF 與 GitHub Pages 為主。
- 不另開新 LINE Bot、不另開新 GAS 架構、不重建使用者入口。
- 正式切換方式是將目前已驗證的程式與 LINE Bot 流程套用到舊的正式 Google Sheets 資料表。
- 稅務專區完整流程暫不執行；正式使用穩定後再進行完整設計與測試。

## 一、UI 全面微調

- [x] 網頁瀏覽器 title 顯示為 `空空 工作台`。
- [x] 頂部協會名稱顯示為 `臺灣撐出空間教育關懷協會`。
- [x] 頂部大字 `空空_工作台` 完全移除。
- [x] 行政人員不得看到「目前沒有可登記的學生或課程」等講師端課程提示。
- [ ] 主功能 TAB 需明確顯示目前所在頁與可點選頁。
- [ ] 行政四大 TAB 維持：學費流程、收據流程、鐘點流程、帳務工具。
- [x] 帳務工具第一層順序為：稅務專區、帳務補救、一般收據、系統工具。
- [x] 點選系統工具後，下方才顯示：單據總覽、查我的 ID。
- [x] 稅務專區點選後只顯示 `建置中`。
- [ ] 手機與電腦版按鈕大小、間距、換行與卡片外框需統一整理。

## 二、重要 ID / 設定外移完整盤點

- [x] 程式與前端設定掃描完成，結果寫入 `doc/重要ID與設定外移清單.md`。
- [x] 新增 `auditProjectProperties()`，可在 GAS 執行必要/建議屬性檢查且不輸出敏感值。
- [x] 新增 `setupRecommendedProjectProperties()`，可將目前 fallback 建議設定補入 Script Properties 且不覆蓋既有值。
- [x] 依 `doc/重要ID與設定外移清單.md` 檢查 GAS Script Properties。
- [x] 在 GAS 編輯器執行 `setupRecommendedProjectProperties()`，將缺少的建議屬性補入 Script Properties。
- [x] 在 GAS 編輯器執行 `auditProjectProperties()`，確認必要屬性缺少 0 項。
- [x] `auditProjectProperties()` 已確認建議屬性缺少 0 項。
- [ ] 在 GAS 編輯器執行 `forceAuth()`，確認四份範本與四個 PDF 資料夾授權正常。
- [ ] 確認正式 `SPREADSHEET_ID` 指向舊正式試算表。
- [ ] 確認 `docs/config.js` 指向正式 Web App、LIFF 與 GitHub Pages 入口。
- [ ] 確認各 Google Docs 範本 ID 與 PDF 資料夾 ID 已在 Script Properties。
- [ ] 確認行政人員、講師、家長 LINE User ID 與 Email 資料來源正確。
- [ ] 正式版前評估是否移除程式碼中的 template/folder/admin fallback。

## 三、正式版切換清單逐項執行

- [ ] 在舊正式 Google Sheets 確認所有必要分頁與欄位存在。
- [ ] 確認 `單據紀錄表`、`學費調整紀錄表`、`一般收據紀錄` 欄位完整。
- [ ] 使用正式試算表執行最小測試：授課登記、預排、核銷、學費流程、收據流程、鐘點流程、帳務工具。
- [ ] 確認 Email 與 LINE push 使用正式資料仍可正常寄送。
- [ ] 確認正式 LINE Bot 好友加入、查 ID、行政/講師綁定流程。
- [ ] 完成正式上線前備份。

## 四、稅務專區完整流程

- [ ] 暫不執行。
- [ ] 正式使用穩定後，再定義捐款申報、年度扣繳、免稅試算、會計日記帳檢核等完整流程。
